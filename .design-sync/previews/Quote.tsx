import { Essay, Kicker, Quote } from 'ajency-design';

export const CashiersAsking = () => (
  <Essay>
    <Kicker>Self-documenting walkthroughs that never go stale</Kicker>
    <p>Then the cashiers started asking:</p>
    <Quote>How do I cancel an incorrect money receipt?</Quote>
  </Essay>
);
