import { Essay, FigureWide } from 'ajency-design';

export const UatFlowFigure = () => (
  <Essay>
    <FigureWide
      src="../../../images/uat-storyboard.webp"
      alt="A six-panel comic: a self-documenting UAT agent walks the app in Chrome, screenshots every step, and turns the click-through into a reusable walkthrough that never goes stale."
      caption="The whole flow, one frame per step."
    />
  </Essay>
);
